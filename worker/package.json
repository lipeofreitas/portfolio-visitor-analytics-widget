{
  "name": "portfolio-visitor-analytics-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute portfolio_visitor_analytics --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute portfolio_visitor_analytics --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
